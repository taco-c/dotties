#!/bin/sh

[ "$1" = "" ] || [ "$2" = "" ] || [ "$3" = "" ] && echo "Usage: $0 <term|gui> <program> <workspace>" && exit

if [ "$(pgrep -x $2)" ]; then
	i3-msg workspace "$3"
else
	[ "$1" = "term" ] && $TERMINAL -e $2 --title $2 || $2
fi

